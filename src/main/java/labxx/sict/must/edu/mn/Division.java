package labxx.sict.must.edu.mn;
public class Division {
    public double divide(double a, double b) {
        // if (b == 0) {
        //     throw new IllegalArgumentException("0-ээр хувааж болохгүй");
        // }
        return a / b;
    }
}